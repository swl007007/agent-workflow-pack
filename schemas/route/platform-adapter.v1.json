{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "agent-workflow.platform-adapter",
  "x-schema-version": 1,
  "x-definition-owner": "task-5",
  "x-implementation-owner": "task-5",
  "x-digest-domains": ["agent-workflow.platform-adapter.v1"],
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_id", "schema_version", "platform", "adapter_id", "adapter_version", "tested_harness_versions", "native_light_entry_id", "caller_context_fields", "capability_probe_suite", "approval_verifiers", "render_projections", "wrapper_entries", "blocked_bypass_entries", "trellis_adapter_contract", "golden_contract_id"],
  "properties": {
    "schema_id": {"const": "agent-workflow.platform-adapter"},
    "schema_version": {"const": 1},
    "platform": {"enum": ["claude-code", "codex", "opencode"]},
    "adapter_id": {"enum": ["claude-code", "codex", "opencode"]},
    "adapter_version": {"type": "string", "minLength": 1},
    "tested_harness_versions": {"type": "array", "minItems": 1, "uniqueItems": true, "items": {"type": "string"}},
    "native_light_entry_id": {"type": "string", "minLength": 1},
    "caller_context_fields": {"type": "array", "uniqueItems": true, "items": {"type": "string"}},
    "capability_probe_suite": {"type": "object"},
    "approval_verifiers": {"type": "object"},
    "render_projections": {"type": "array", "items": {"type": "object"}},
    "wrapper_entries": {"type": "array", "items": {"type": "object"}},
    "blocked_bypass_entries": {"type": "array", "uniqueItems": true, "items": {"type": "string"}},
    "trellis_adapter_contract": {"type": "object"},
    "golden_contract_id": {"type": "string", "minLength": 1}
  }
}
